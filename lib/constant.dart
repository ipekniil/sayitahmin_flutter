import 'package:flutter/material.dart';

const textStyle =
    TextStyle(fontSize: 20.0, fontFamily: 'Josef', color: Colors.white);

const hintStyle = TextStyle(
  color: Colors.white,
);

const pikachuTalkTextStyle =
    TextStyle(fontFamily: 'Josef', color: Colors.white, fontSize: 25.0);
const ruleText =
    TextStyle(fontFamily: 'Josef', color: Colors.pink, fontSize: 25.0);

const animationTitle = TextStyle(fontSize: 40.0, fontFamily: "Josef");

const bottomButtonTextStyle = TextStyle(
  fontSize: 20.0,
  fontFamily: 'Josef',
  color: Colors.black,
  shadows: <Shadow>[
    Shadow(
      blurRadius: 15.0,
      color: Colors.white,
    ),
    Shadow(
      blurRadius: 15.0,
      color: Colors.white,
    ),
  ],
);

const bottomButtonAgainTextStyle = TextStyle(
  fontSize: 20.0,
  fontFamily: 'Josef',
  color: Colors.black,
  shadows: <Shadow>[
    Shadow(
      blurRadius: 15.0,
      color: Colors.white,
    ),
    Shadow(
      blurRadius: 15.0,
      color: Colors.white,
    ),
  ],
);
